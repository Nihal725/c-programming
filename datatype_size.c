#include<stdio.h>
int main ()
{
   int i;
   float f;
   double d;
   char c;

   printf("size of int = %dbytes\nsize of float = %dbytes\nsize of double = %dbytes\nsize of char = %dbytes",sizeof(i),sizeof(f),sizeof(d),sizeof(c));
    return 0;



}